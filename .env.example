OPENAI_API_KEY=...
# gpt-4o recommande (recherche web integree via Responses API)
OPENAI_MODEL=gpt-4o

# Vercel Blob (obligatoire en production sur Vercel)
# Cree un store Blob dans le dashboard Vercel → Storage → Blob
# BLOB_READ_WRITE_TOKEN est injecte automatiquement
